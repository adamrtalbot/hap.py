# qfy matrix fixtures

The qfy matrix currently derives its shared GA4GH intermediate from the pinned
chr21 truth/query inputs in `samplesheet.qfy.csv`. This directory reserves the
local-fixture namespace for small qfy-specific region and annotation inputs
when the qfy process accepts a directly staged intermediate VCF.
